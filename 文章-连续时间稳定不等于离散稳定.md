# 连续时间稳定 ≠ 离散稳定

> 配套交互演示：`web/index.html`（双击即可打开） · 数值证据：`web/test-core.js`（25 项断言）
> 理论底稿：`README.md` · 实现：`ode_incremental_stability.py`

---

## 0. 一句话

把增量式算法写成连续时间 ODE，只完成了分析的一半：

$$\text{算法} \;=\; \underbrace{\text{某个向量场 } h}_{\text{ODE}}\;+\;\underbrace{\text{某个积分格式}}_{\text{步长上界来自这里}}$$

ODE 的特征值只要落在**左半平面**就稳定，而离散迭代要求 $\eta\lambda$ 落在一个**有限的圆盘**里。
这两者之间的缝隙，就是"调小学习率也救不回来"的那些不稳定的来源。

---

## 1. 一个反复出现的误判

流程通常是这样的：

1. 写出平均场方向场 $h(x)=\mathbb{E}[H(x,\xi)]$，得到 ODE $\dot x = h(x)$；
2. 线性化，算出 Jacobian $J$ 的特征值，发现全都满足 $\operatorname{Re}\lambda<0$；
3. 宣布"算法收敛"，把 $\eta$ 按经验往大调；
4. 实验发散。

第 4 步不是运气不好。**连续时间的稳定性是离散时间稳定性的必要条件，不是充分条件。**
两个稳定域的形状完全不同：一个是无限大的半平面，一个是有限的圆盘。

---

## 2. 几何：从半平面到圆盘

线性化后迭代是

$$x_{k+1} = (I+\eta J)\,x_k ,$$

这正是一步**前向 Euler**。逐模态看，$x_k \propto (1+\eta\lambda)^k$，所以

$$|1+\eta\lambda| < 1 \quad\Longleftrightarrow\quad \Big|\lambda + \tfrac1\eta\Big| < \tfrac1\eta .$$

左边是"乘子落在单位圆内"，右边是复平面上**以 $-1/\eta$ 为圆心、半径 $1/\eta$** 的圆盘 ——
它在原点与虚轴相切，整个身子待在左半平面里。

- $\eta$ 越小，圆盘越大，容错越宽；
- $\eta$ 越大，圆盘越小，先被挤出去的永远是**离原点最远**（$|\lambda|$ 最大）的模态。

打开 $|1+\eta\lambda|^2 = 1 - 2\eta\operatorname{Re}\lambda + \eta^2|\lambda|^2$，与 1 比较：

$$\eta\,|\lambda|^2 < 2\operatorname{Re}\lambda
\quad\Longleftrightarrow\quad
\eta < \frac{2\operatorname{Re}\lambda}{|\lambda|^2}.$$

若我们站在 ODE 一侧（$J$ 是 Hurwitz 的，$\operatorname{Re}\lambda<0$），把符号翻过来即

$$\boxed{\;\eta_{\max} = \min_i \frac{2\,(-\operatorname{Re}\lambda_i)}{|\lambda_i|^2}\;}\qquad
\text{实特征值特例：}\;\eta_{\max} = \frac{2}{\lambda_{\max}} .$$

注意分子是 $-\operatorname{Re}\lambda$（距离虚轴的距离），分母是 $|\lambda|^2$（离原点的远近）。
所以**同样是"稳定的"特征值，越大、越靠近虚轴的那些越凶**。

---

## 3. 代数：三个最常用的情形

### 3.1 线性迭代 / 二次目标的梯度下降

$$x \leftarrow x - \eta(Ax-b),\qquad \dot x = -A(x-x^*).$$

$\lambda(A)>0$ 时 ODE 无条件稳定，但离散要求 $\eta < 2/\lambda_{\max}$。
**"ODE 没有步长"这句话本身就是重点**：步长限制是格式带来的，不是问题带来的。

### 3.2 Heavy-ball / momentum

$$x_{k+1} = x_k - \eta\nabla f(x_k) + \beta(x_k - x_{k-1}).$$

连续极限（时间尺度 $t=k\sqrt\eta$）是

$$\ddot x + \frac{1-\beta}{\sqrt\eta}\,\dot x + \nabla f = 0 ,$$

阻尼 $a=(1-\beta)/\sqrt\eta>0 \iff \beta<1$，能量 $\dot E = -a\|\dot x\|^2 \le 0$：
**只要 $\beta<1$，ODE 无条件稳定，与 $\eta$ 无关。**

离散侧逐模态得到 $\mu^2 - (1+\beta-\eta\lambda)\mu + \beta = 0$，Jury/Schur 判据给出

$$|\beta|<1,\qquad 0 < \eta\lambda < 2(1+\beta)
\quad\Longrightarrow\quad
\eta_{\max}(\beta) = \frac{2(1+\beta)}{\lambda_{\max}} .$$

两条值得记住的结构：

- **判别式 $<0$（复根区）时 $|\mu|=\sqrt\beta$，与 $\eta$ 完全无关** —— 迭代以固定几何速率振荡收敛，动量越大衰减越慢；
- **$\eta_{\max}$ 随 $\beta$ 线性放大 $(1+\beta)$ 倍** —— 这就是"加速"的定价：用更慢的振荡换更大的可用步长。

### 3.3 噪声：ODE 管零点，格式管方差

噪声不移动 $h$ 的零点，所以不改变平衡点；它决定误差停在哪里。标量 LMS
$\theta_{k+1}=\theta_k+\eta\phi(y-\phi^\top\theta)$ 的误差递推是
$e_{k+1}=(1-\eta s^2)e_k+\eta s\,\varepsilon$，稳态方差有闭式

$$V = \frac{\eta^2 s^2\sigma^2}{1-(1-\eta s^2)^2} = \frac{\eta\,\sigma^2}{2-\eta s^2}
\;\xrightarrow[\eta\to0]{}\; \frac{\eta\sigma^2}{2}.$$

**正比于 $\eta$**，于是常数步长的误差永久停在 $O(\sqrt\eta)$ 的噪声地板上；
换成衰减步长（$\sum\eta=\infty,\ \sum\eta^2<\infty$）才从"停住"变成"收敛到点"。

---

## 4. 数值证据（本仓库现场跑出来的）

下表由 `node test-core.js` 输出（同一份内核被网页 §05「自检」复用），共 **25 项断言，全部通过**：

| 检验项 | 理论值 | 实测 | 相对误差 |
|---|---|---|---|
| A · $\eta_{\max}=2/\lambda_{\max}$（解析 vs 二分） | 0.5 | 0.5 | 0 |
| A · $\rho(I-\eta A)$，$\eta=0.025/0.075/0.125/0.2$ | 0.975 / 0.925 / 0.875 / 0.8 | 逐位吻合 | 0 |
| A · 边界两侧 $0.99\eta_{\max}$ 收敛 / $1.01\eta_{\max}$ 发散 | — | $2.83\times10^{-18}$ / $1.01\times10^{12}$ | — |
| A · 复特征值 $\lambda=-1\pm3i$ 的 $\eta_{\max}=2/\|\lambda\|^2$ | 0.2 | 0.2 | 0 |
| B · $\eta_{\max}(\beta)=2(1+\beta)/\lambda_{\max}$，$\beta=0,\dots,0.9$ | 1.0 / 1.2 / 1.5 / 1.8 / 1.9 | 数值二分吻合 | ≤ $1.9\times10^{-16}$ |
| B · 复根区 $\rho=\sqrt\beta$ | 0.707107 | 0.707107 | 0 |
| B · 实测几何增长率 vs $\rho$（几何平均） | 0.707107 | 0.706989 | $1.7\times10^{-4}$ |
| B · **GAP：ODE 阻尼 $a>0$ 而离散爆炸** | $a=0.3333>0$ | 第 **27** 步 $\|x\|>10^{12}$，$\rho=2.8229$ | — |
| (5) · Lyapunov $J^\top P+PJ=-I$，$J=\mathrm{diag}(-1,-4)$ | $P=\mathrm{diag}(0.5,0.125)\succ0$ | 残差 $\sim0$ | — |
| C · 稳态方差 $\eta\sigma^2/(2-\eta s^2)$ vs 蒙特卡洛 | 0.025641 | 0.025360 | $1.1\times10^{-2}$ |
| C · 小 $\eta$ 渐近 $V/(\eta\sigma^2/2)\to1$ | 1 | 1.001001 | $1.0\times10^{-3}$ |
| C · 双对数斜率 $\mathrm{d}\log \mathrm{MSD}/\mathrm{d}\log\eta$ | 1 | 1.023823 | $2.4\times10^{-2}$ |
| C · 衰减步长 $\eta_k=2/(k+10)$：$\mathrm{MSD}\cdot k\to c/2$ | 1 | 1.294（末值 $4.3\times10^{-4}$，同刻地板 0.1111） | $2.9\times10^{-1}$ |
| C · 前向 Euler 全局误差 $=O(\eta)$，$\mathrm{err}/\eta$ | 常数 | 0.18787 / 0.18471 / 0.18409 | $3.3\times10^{-3}$ |

两点统计上的诚实说明：

- 蒙特卡洛的 $\pm1.96\,\mathrm{SE}$ 是**按时间步算的标准误**，忽略了 MSD 序列的自相关，
  因此系统性偏小（0.025360 与 0.025641 的差就超出了这个区间）。要出置信区间应当用批量均值（batch means）或
  HAC 估计 —— 这属于评估方法论，不属于 ODE。
- 表格里"实测"一列是**可复现**的：固定种子，任何人跑同一个命令得到同一张表。

---

## 5. 四个可交互演示

打开 `web/index.html`（无构建、无网络依赖）：

| 演示 | 它在说什么 |
|---|---|
| **① 时间重标定** | 把离散点画在 $t=k\eta$ 上，整条落在 ODE 流上；关掉重标定，两条曲线立刻差一个 $\eta$ 的时间尺度 |
| **② 稳定域** | 左半平面 vs 圆盘（可拖动 $\lambda$ 点）；橙色点就是"连续稳定但离散不稳定"的缝隙 |
| **③ Hopf 分岔** | Heavy-ball 的根轨迹随 $\eta$ 移动、穿越单位圆；右边是同步的实际迭代轨迹 |
| **④ 噪声地板** | 双对数图上 MSD 斜率 1；常数步长停在 $O(\sqrt\eta)$，衰减步长继续下降 |

---

## 6. 三条可以直接用的设计规则

1. **$\eta_{\max}$ 不必扫参，有闭式。** 一阶方法 $\eta<2/\lambda_{\max}$；
   Heavy-ball $\eta<2(1+\beta)/\lambda_{\max}$。$\lambda_{\max}$ 可用幂迭代 / Hessian-向量积估出来，
   于是"学习率上限"从实验结果变成先验量。
2. **病态与刚性是同一件事。** $\kappa=\lambda_{\max}/\lambda_{\min}$ 的病态问题就是刚性 ODE：
   步长被 $\lambda_{\max}$ 卡住，迭代次数被 $\kappa$ 卡住。出路只有两条且互不替代 ——
   **换变量**（预条件 / 自然梯度 / 白化，改度量）或**换向量场**（加动量、加高阶项、Nesterov）。
3. **要"无条件稳定"就换格式。** 后向 Euler / proximal（对应 proximal point、ADMM）是 A-稳定的，
   对任何 $\eta$ 都稳，代价是每步解一个子问题。保能量用辛格式，抗刚性用隐式格式 ——
   数值 ODE 一百年的工具箱可以整箱搬过来。

---

## 7. 它买不到的东西

- **GAP 反例（本文实测）**：$\beta=0.5$、$\lambda_{\max}=2$、$\eta=2.25$ 时连续阻尼 $a=0.3333>0$，
  ODE 无可争议地稳定，而离散迭代在第 27 步就冲过 $10^{12}$。**"连续稳定"从来没有承诺过"离散稳定"。**
- **非正规矩阵的瞬态增长**：$\rho(M)<1$ 不保证误差单调下降，可能有巨大的瞬时放大。
  `README.md` §6 里那个 $10^{140}$ 的数值例子（定点迭代解稳态协方差方程把 $10^{-16}$ 的舍入放大）就是它 ——
  这时要上伪谱 / 数值域，而不是特征值。
- **非光滑**（ReLU、L1、投影、clipping）：ODE 升级为**微分包含** $\dot x \in H(x)$，
  要用 Clarke 广义梯度、单调算子 / 预解式；分裂算法（ADMM、Douglas–Rachford）在这套语言下最自然。
- **大 $\eta$**（$\eta\lambda_{\max}$ 接近 2）：恰恰是 ODE 逼近失效的区间，而实际调参常落在这里。
  此时 ODE 只给"趋势正确、细节失真"。
- **精确方差与有限样本界**：ODE 只给均值场。要置信区间、中心极限定理、有限样本界，需上扩散逼近 / SDE。

**一句话取舍**：ODE 分析换来的是"可迁移的机制理解 + 解析的设计规则"，
代价是失去对有限步长、精确方差和全局非凸行为的控制。它替代的是盲目扫参，不是替代实验。

---

## 8. 同一个判据的另外两张面孔

- **随机逼近**：把随机迭代取条件期望得到 $h$，Kushner–Clark / Robbins–Monro 定理说明
  "ODE 稳定 $\Rightarrow$ 算法收敛"（需 $\sum\eta=\infty,\ \sum\eta^2<\infty$）。缺了衰减条件，
  迭代只会停在平衡点附近 $O(\sqrt\eta)$ 的邻域里而不收敛 —— 与第 3.3 节是同一件事。
- **RL 的 TD 学习**：漂移矩阵 $A_\nu$ 的特征值实部符号就是稳定性判据
  （$A_\nu$ 正稳定 $\iff \mathbb{E}[\theta_t]\to\theta^\star$），而 Lyapunov 形式
  $A^\top P + PA \prec 0$ 正是本文 §4 里那个 $P=\mathrm{diag}(0.5,0.125)$ 的推广。
  详见 `RL-收敛性与样本复杂度-讲义.md` §4.6 / §4.7 的最小发散反例。

---

## 9. 复现

```bash
cd incremental-ode/web
node test-core.js     # 25 项断言 + 数值证据表
node smoke-dom.js     # 17 项：渲染路径与交互的冒烟测试
# 然后双击 index.html 看四个演示
```

部署到 GitHub Pages 的三种做法见 `web/README.md`。

---

## 10. 延伸阅读

| 方向 | 文献 |
|---|---|
| ODE 方法 / 随机逼近 | Ljung (1977)；Kushner & Clark (1978)；Benveniste–Métivier–Priouret (1990)；Borkar (2008) |
| 连续化 / 梯度流 | Polyak (1964)；Su–Boyd–Candès (2014)；Wibisono–Wilson–Jordan (2016)；Shi et al. (2018) |
| ODE 数值解与绝对稳定性 | Dahlquist；Hairer & Wanner；Lax 等价定理 |
| 动力系统与稳定性判据 | Khalil *Nonlinear Systems*；Sastry *Nonlinear Systems* |
| 多时间尺度 | Borkar；Konda–Tsitsiklis (1999) |
